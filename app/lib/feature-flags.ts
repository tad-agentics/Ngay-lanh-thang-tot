/** Temporary product toggles — flip to re-enable without redeploying routes. */

/** Tab Tra cứu · Hợp tuổi (`/tra-cuu/hop-tuoi`). */
export const HOP_TUOI_ENABLED = false;

/** Luận lưu niên & lưu nguyệt (`/toi/luan-tieu-van`, addon `luan_tieu_van`). */
export const TIEU_VAN_LUAN_ENABLED = false;
